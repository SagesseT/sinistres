{% extends 'base.html.twig' %}

{% block title %}Avenue{% endblock %}

{% block body %}
<main id="main" class="main">
    <h1>Avenue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avenue.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ avenue.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avenue_index') }}">back to list</a>

    <a href="{{ path('app_avenue_edit', {'id': avenue.id}) }}">edit</a>

    {{ include('avenue/_delete_form.html.twig') }}
      </main>
{% endblock %}
