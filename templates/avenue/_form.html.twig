{{ form_start(form) }}

<div class="mb-3">
    <label class="form-label">Nom de l'avenue</label>
    {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
</div>

<div class="d-flex justify-content-between mt-3">
    <a href="{{ path('app_avenue_index') }}" class="btn btn-secondary">
        ⬅ Retour
    </a>

    <button class="btn btn-success">
        💾 Enregistrer
    </button>
</div>

{{ form_end(form) }}